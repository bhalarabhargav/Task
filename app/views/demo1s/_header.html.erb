<!DOCTYPE html>
<html lang="en">
<head>
  <title>Task</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow-x: hidden;
    }

    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    .content {
      margin-top: 60px;
      padding: 15px;
    }

    .custom-button-spacing {
      padding-top: 10px;
      padding-bottom: 10px;
    }

 @media (max-width: 768px) {
  .navbar-nav .nav-item .btn {
    all: unset; /* Reset all styles */
    color: white; /* Ensure text remains visible */
    cursor: pointer; /* Keep it clickable */
    display: block; /* Optional: Makes sure text stacks neatly */
  }
}
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <!-- Navbar Toggle Button (for mobile view) -->
      <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
        <span class="navbar-toggler-icon ms-auto"></span>
      </button>

      <!-- Navbar Items -->
      <div class="offcanvas offcanvas-end bg-dark text-white " tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
        <div class="offcanvas-header ms-auto">
          <h5 class="offcanvas-title" id="offcanvasNavbarLabel">Menu</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        </div>
        <div class="offcanvas-body">
          <ul class="navbar-nav custom-button-spacing ms-auto">
            <li class="nav-item">
              <%= link_to "Home", demo1s_path, class: "btn btn-primary" %>
            </li>
            <li class="nav-item">
              <%= link_to "New-Task", new_demo1_path, class: "btn btn-primary" %>
            </li>
            <li class="nav-item">
              <%= button_to "Log out", user_session_path(current_user), method: :delete, class: "btn btn-primary" %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </nav>

  <!-- Add Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
